
### read in CEL files, frma preprocess ###

frma.6791 <- exprs(frma(dat.6791))
ProjectTemplate::cache('frma.6791')

frma.3292 <- exprs(frma(dat.3292))
ProjectTemplate::cache('frma.3292')

frma.9844 <- exprs(frma(dat.9844))
ProjectTemplate::cache('frma.9844')
