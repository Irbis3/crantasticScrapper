# data

library(dplyr)
library(httr) # not used, constituencies not checked
library(stringr)
library(XML)

# networks

library(rgexf) # keep first
library(network)
library(sna)

# plots

library(ggplot2)
library(grid)
